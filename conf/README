This directory contains files with information to configure
the build of ITS.

network - Network configuration.
hosts - Added to the hosts table. It uses the H3TEXT format (refer to
        build/h3text.*; the h3text that ends up in ITS is a combination of that
        file and hosts).
