(defun MAKOBLIST (x)
  (cond ((null x)
         (LISTARRAY obarray (- (cadr (arraydims 'obarray)) 129.)))
        (t 
         (*ARRAY x 'OBARRAY))))
                        
(setq car t)
(setq cdr t)

(defun dump-planner ()
  (suspend)
  (thinit)
  (thert toplevel))
