

(SETQ NODE-STOP NIL SMN-STOP NIL)

(SETQ ALTMODE (LIST (ASCII 27.)))

(SETQ BREAKCHARS (LIST (ASCII 32.) (ASCII 13.) (ASCII 46.)))

(LABELTRACE CLAUSE NG VG ADJG PREPG CONJOIN)

(SSTATUS SYNTAX 13. 163840.)

(SETQ LINEL 65.)

(SETQ DPSTOP NIL)

(SETQ =LINE
      '====================================================
      (SETQ MASTER-SWITCHES '(NOSTOP DPSTOP
				     PARSEBREAK
				     LABELBREAKANSWER?
				     DOIT
				     DISCOURSE
				     NOMEM
				     IASSUME
				     PLANNERSEE
				     BUILDSEEPARSENODE-SEE))
      (SETQ REGISTERS '(SMSUB SMOB1 SMOB2 SOBL SMCOMP))
      (MAPC '(LAMBDA (X) (SETQ X '<EMPTY>)) REGISTERS))

(MAPC 
 '(LAMBDA (X) (PUTPROP X 'OFF 'TURNED) MASTERSWITCHES)
 
ILGL MEMORY REFERENCE FROM LOC 73230
PROGRAM TRAPPED WHILE IN MEM-