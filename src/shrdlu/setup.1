(SETQ NODE-STOP NIL SMN-STOP NIL)

(SETQ ALTMODE (LIST (ASCII 33)))

(SETQ BREAKCHARS (LIST (ASCII 40) (ASCII 15) (ASCII 56)))

(LABELTRACE CLAUSE NG VG ADJG PREPG CONJOIN)

(SSTATUS SYNTAX 15 500000)

(SETQ LINEL 65.)

(SETQ DPSTOP NIL)

(SETQ =LINE '====================================================

(SETQ MASTER-SWITCHES '(NOSTOP DPSTOP PARSEBREAK LABELBREAK
ANSWER? DOIT DISCOURSE NOMEM IASSUME PLANNERSEE BUILDSEE
PARSENODE-SEE))

(SETQ REGISTERS '(SMSUB SMOB1 SMOB2 SOBL SMCOMP))

(MAPC '(LAMBDA (X) (SETQ X '<EMPTY>)) REGISTERS))

(MAPC '(LAMBDA (X) (PUTPROP X 'OFF 'TURNED) MASTERSWITCHES))

(SETQ ZOGUSER NIL)

(SETQ ERRLIST '((SETQ ERRLIST '((SHRDLU)) (ERTERR SHRDLU COMMAND STATE/,
    TYPE HELP<CR> FOR INSTRUCTIONS/.)))

(*RSET T)
